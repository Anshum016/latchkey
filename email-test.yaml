notifications:
  channel: email
  webhookBaseUrl: http://localhost:3001
  timeoutMs: 180000

upstreams:
  - name: mock
    transport: stdio
    command: node
    args:
      - packages/mcp/build/mock-server-v2.js

proxy:
  toolNameMode: transparent
